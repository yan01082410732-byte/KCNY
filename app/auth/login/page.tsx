import { signIn } from "../actions"; import { normalizeLanguage,safeReturnTo } from "@/lib/auth"; import { message } from "@/lib/auth-messages"; import { AuthSubmitButton } from "@/components/AuthSubmitButton";
export default async function Page({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const p=await searchParams;const language=normalizeLanguage(p.lang),korean=language==="KR";return <main className="auth-page"><section className="auth-card"><h1>{korean?"로그인":"登录"}</h1>{p.notice==="check_email"&&<p className="auth-notice">{korean?"이메일을 확인해 주세요.":"请查收验证邮箱。"}</p>}{p.error&&<p className="auth-error">{message(p.error,language)}</p>}<form action={signIn}><input type="hidden" name="language" value={language}/><input type="hidden" name="returnTo" value={safeReturnTo(p.returnTo)}/><label htmlFor="email">{korean?"이메일":"邮箱"}<input id="email" name="email" type="email" autoComplete="email" required/></label><label htmlFor="password">{korean?"비밀번호":"密码"}<input id="password" name="password" type="password" autoComplete="current-password" required/></label><AuthSubmitButton idleText={korean?"로그인":"登录"} pendingText={korean?"로그인 중…":"登录中……"}/></form><a href={`/auth/signup?lang=${language}`}>{korean?"회원가입":"注册"}</a></section></main>}
