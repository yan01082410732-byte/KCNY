import { signIn } from "../actions"; import { message } from "@/lib/auth-messages";
export default async function Page({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const p=await searchParams,l=p.lang==="ko"?"ko":"zh",k=l==="ko";return <main className="auth-page"><section className="auth-card"><a className="brand" href="/"><span>K</span>CNY</a><h1>{k?"로그인":"登录"}</h1>{p.notice&&<p className="auth-notice">{k?"이메일을 확인해 주세요.":"请查收验证邮箱。"}</p>}{p.error&&<p className="auth-error">{message(p.error,l)}</p>}<form action={signIn}><input type="hidden" name="language" value={l}/><input type="hidden" name="returnTo" value={p.returnTo||"/"}/><label>{k?"이메일":"邮箱"}<input name="email" type="email" required autoComplete="email"/></label><label>{k?"비밀번호":"密码"}<input name="password" type="password" required autoComplete="current-password"/></label><button className="button primary">{k?"로그인":"登录"}</button></form><p><a href={`/auth/signup?lang=${l}`}>{k?"회원가입":"注册"}</a></p></section></main>}
