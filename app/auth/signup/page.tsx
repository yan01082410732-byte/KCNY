import { signUp } from "../actions"; import { message } from "@/lib/auth-messages";
export default async function Page({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const p=await searchParams,l=p.lang==="ko"?"ko":"zh",k=l==="ko";return <main className="auth-page"><section className="auth-card"><a className="brand" href="/"><span>K</span>CNY</a><h1>{k?"회원가입":"注册"}</h1>{p.error&&<p className="auth-error">{message(p.error,l)}</p>}<form action={signUp}><input type="hidden" name="language" value={l}/><label>{k?"사용자 이름":"用户名"}<input name="username" required minLength={2} maxLength={30} autoComplete="username"/></label><label>{k?"표시 이름":"显示名称"}<input name="displayName" required maxLength={50}/></label><label>{k?"이메일":"邮箱"}<input name="email" type="email" required autoComplete="email"/></label><label>{k?"비밀번호":"密码"}<input name="password" type="password" required minLength={8} autoComplete="new-password"/></label><label>{k?"비밀번호 확인":"确认密码"}<input name="confirmPassword" type="password" required minLength={8} autoComplete="new-password"/></label><button className="button primary">{k?"가입하기":"注册"}</button></form><p><a href={`/auth/login?lang=${l}`}>{k?"로그인":"登录"}</a></p></section></main>}
