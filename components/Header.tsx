"use client";

export function Header({ language, onLanguageChange, authenticated, username, displayName }: { language: "CN" | "KR"; onLanguageChange: (language: "CN" | "KR") => void; authenticated: boolean; username?: string; displayName?: string }) {
  const korean = language === "KR";
  return <header><div className="shell nav"><a className="brand" href="/"><span>K</span>CNY</a><nav><a href="#feed">Community</a><a href="/coming-soon">Discover</a><a href="/coming-soon">About</a></nav><div className="nav-actions"><div className="language"><button className={language === "CN" ? "selected" : ""} onClick={() => onLanguageChange("CN")}>中</button><button className={korean ? "selected" : ""} onClick={() => onLanguageChange("KR")}>한</button></div>{authenticated ? <><span className="user-name">{displayName || username || (korean ? "회원" : "用户")}</span><a href="/coming-soon">{korean ? "프로필" : "个人主页"}</a><a className="signin" href="/coming-soon">{korean ? "글 쓰기" : "发布帖子"}</a><form action="/auth/signout" method="post"><button className="signin secondary-link">{korean ? "로그아웃" : "退出"}</button></form></> : <><a className="signin" href={`/auth/login?lang=${language}`}>{korean ? "로그인" : "登录"}</a><a className="signin secondary-link" href={`/auth/signup?lang=${language}`}>{korean ? "회원가입" : "注册"}</a></>}</div></div></header>;
}
